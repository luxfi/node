# chains/cpp — Phase 2 placeholder

Empty. Port target: the P/X/Q/Z chains, ported from `chains/go`'s reference
(`~/work/lux/chains`) into `~/work/lux-cpp/node`'s VM seam
(`include/lux/node/vm.hpp`). `runtime/cpp` already runs a C-Chain today via
`cevm` (see its README) — this is the rest of the chain suite, in C++, behind
the same seam.
