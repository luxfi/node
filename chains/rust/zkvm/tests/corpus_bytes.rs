// SPDX-License-Identifier: BSD-3-Clause-Eco
// Copyright (C) 2019-2026, Lux Industries Inc. All rights reserved.

//! Every Z vector in the shared corpus, written back byte for byte.
//!
//! The corpus was generated by the Go chain, so a vector's hex is what Go
//! wrote. Reading one and writing it again has to reproduce it exactly: this
//! chain's writer and Go's are the same writer or they are not. That is what
//! makes the corpus a check on the WRITE path and not only on the read path —
//! a reader can agree about every field of bytes it could never have produced.

use std::path::PathBuf;

use lux_zkvm::wire;

fn corpus() -> String {
    let p = PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("../../../conformance/corpus/vectors.tsv");
    std::fs::read_to_string(&p).unwrap_or_else(|e| panic!("{}: {e}", p.display()))
}

fn unhex(s: &str) -> Option<Vec<u8>> {
    if s == "-" || s.len() % 2 != 0 {
        return None;
    }
    (0..s.len() / 2)
        .map(|i| u8::from_str_radix(&s[i * 2..i * 2 + 2], 16).ok())
        .collect()
}

#[test]
fn every_z_vector_is_written_back_exactly_as_it_arrived() {
    let text = corpus();
    let mut blocks = 0;
    for line in text.lines() {
        if line.is_empty() || line.starts_with('#') {
            continue;
        }
        let f: Vec<&str> = line.split('\t').collect();
        if f.len() < 5 || f[0] != "V" || f[2] != "Z" || f[3] != "block" {
            continue;
        }
        let id = f[1];
        let Some(bytes) = unhex(f[4]) else { continue };
        let Ok(body) = wire::read_block(&bytes) else {
            continue;
        };
        assert_eq!(wire::write_block(&body), bytes, "{id}: block bytes moved");
        blocks += 1;
    }
    // A corpus that stopped naming Z vectors would pass every assertion above.
    assert!(blocks > 0, "no Z block vector was read back");
    println!("{blocks} Z blocks written back byte for byte");
}
