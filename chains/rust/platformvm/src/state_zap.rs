// Code generated by zapgen; DO NOT EDIT.
// source: state.zap

//! ZAP schema `state.zap`, package `state`.
//!
//! Every view here borrows the buffer it reads: a field is a bounds-checked
//! look at bytes already in hand, never a decode into an owned struct. The
//! builders write into one buffer and hand it over at the end.

#![allow(dead_code)]

use lux_zap::zap;

// Owners — field offsets, in bytes, and the fixed section's size.
pub const OWNERS_LOCKTIME: usize = 0;
pub const OWNERS_THRESHOLD: usize = 8;
pub const OWNERS_ADDRESSES: usize = 16;
pub const OWNERS_SIZE: usize = 24;

/// A view of a ZAP-encoded Owners. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Owners<'a> {
    o: zap::Object<'a>,
}

impl<'a> Owners<'a> {
    /// Take `data` as a Owners message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Owners {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Owners { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn locktime(&self) -> u64 {
        self.o.u64(OWNERS_LOCKTIME)
    }

    pub fn threshold(&self) -> u32 {
        self.o.u32(OWNERS_THRESHOLD)
    }

    pub fn addresses(&self) -> &'a [u8] {
        self.o.bytes(OWNERS_ADDRESSES)
    }
}

/// The field values [`new_owners`] writes.
#[derive(Clone, Copy, Debug)]
pub struct OwnersInput<'a> {
    pub locktime: u64,
    pub threshold: u32,
    pub addresses: &'a [u8],
}

impl<'a> Default for OwnersInput<'a> {
    fn default() -> Self {
        OwnersInput {
            locktime: 0,
            threshold: 0,
            addresses: &[],
        }
    }
}

/// Write a Owners into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_owners(b: &mut zap::Builder, input: &OwnersInput<'_>) -> usize {
    let mut ob = b.start_object(OWNERS_SIZE);
    ob.set_u64(b, OWNERS_LOCKTIME, input.locktime);
    ob.set_u32(b, OWNERS_THRESHOLD, input.threshold);
    ob.set_bytes(b, OWNERS_ADDRESSES, input.addresses);
    ob.finish(b)
}

/// Write a Owners message and answer its bytes.
pub fn new_owners(input: &OwnersInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_owners(&mut b, input);
    b.set_root(at);
    b.finish()
}

// Staker — field offsets, in bytes, and the fixed section's size.
pub const STAKER_WEIGHT: usize = 0;
pub const STAKER_START: usize = 8;
pub const STAKER_END: usize = 16;
pub const STAKER_REWARD: usize = 24;
pub const STAKER_NEXT: usize = 32;
pub const STAKER_PRIORITY: usize = 40;
pub const STAKER_HAS_KEY: usize = 41;
pub const STAKER_PUBLIC_KEY: usize = 48;
pub const STAKER_RESERVED: usize = 56;
pub const STAKER_SIZE: usize = 72;

/// A view of a ZAP-encoded Staker. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Staker<'a> {
    o: zap::Object<'a>,
}

impl<'a> Staker<'a> {
    /// Take `data` as a Staker message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Staker {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Staker { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn weight(&self) -> u64 {
        self.o.u64(STAKER_WEIGHT)
    }

    pub fn start(&self) -> u64 {
        self.o.u64(STAKER_START)
    }

    pub fn end(&self) -> u64 {
        self.o.u64(STAKER_END)
    }

    pub fn reward(&self) -> u64 {
        self.o.u64(STAKER_REWARD)
    }

    pub fn next(&self) -> u64 {
        self.o.u64(STAKER_NEXT)
    }

    pub fn priority(&self) -> u8 {
        self.o.u8(STAKER_PRIORITY)
    }

    pub fn has_key(&self) -> u8 {
        self.o.u8(STAKER_HAS_KEY)
    }

    pub fn public_key(&self) -> &'a [u8] {
        self.o.bytes(STAKER_PUBLIC_KEY)
    }

    /// The 16 inline bytes at `STAKER_RESERVED`; zeros if the span runs off the buffer.
    pub fn reserved(&self) -> &'a [u8; 16] {
        self.o
            .bytes_fixed(STAKER_RESERVED, 16)
            .try_into()
            .unwrap_or(&[0u8; 16])
    }
}

/// The field values [`new_staker`] writes.
#[derive(Clone, Copy, Debug)]
pub struct StakerInput<'a> {
    pub weight: u64,
    pub start: u64,
    pub end: u64,
    pub reward: u64,
    pub next: u64,
    pub priority: u8,
    pub has_key: u8,
    pub public_key: &'a [u8],
    pub reserved: &'a [u8; 16],
}

impl<'a> Default for StakerInput<'a> {
    fn default() -> Self {
        StakerInput {
            weight: 0,
            start: 0,
            end: 0,
            reward: 0,
            next: 0,
            priority: 0,
            has_key: 0,
            public_key: &[],
            reserved: &[0u8; 16],
        }
    }
}

/// Write a Staker into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_staker(b: &mut zap::Builder, input: &StakerInput<'_>) -> usize {
    let mut ob = b.start_object(STAKER_SIZE);
    ob.set_u64(b, STAKER_WEIGHT, input.weight);
    ob.set_u64(b, STAKER_START, input.start);
    ob.set_u64(b, STAKER_END, input.end);
    ob.set_u64(b, STAKER_REWARD, input.reward);
    ob.set_u64(b, STAKER_NEXT, input.next);
    ob.set_u8(b, STAKER_PRIORITY, input.priority);
    ob.set_u8(b, STAKER_HAS_KEY, input.has_key);
    ob.set_bytes(b, STAKER_PUBLIC_KEY, input.public_key);
    ob.set_bytes_fixed(b, STAKER_RESERVED, input.reserved);
    ob.finish(b)
}

/// Write a Staker message and answer its bytes.
pub fn new_staker(input: &StakerInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_staker(&mut b, input);
    b.set_root(at);
    b.finish()
}

// L1Validator — field offsets, in bytes, and the fixed section's size.
pub const L1_VALIDATOR_CHAIN: usize = 0;
pub const L1_VALIDATOR_NODE_ID: usize = 32;
pub const L1_VALIDATOR_START: usize = 56;
pub const L1_VALIDATOR_WEIGHT: usize = 64;
pub const L1_VALIDATOR_MIN_NONCE: usize = 72;
pub const L1_VALIDATOR_END_FEE: usize = 80;
pub const L1_VALIDATOR_PUBLIC_KEY: usize = 88;
pub const L1_VALIDATOR_REMAINING_OWNER: usize = 96;
pub const L1_VALIDATOR_DISABLE_OWNER: usize = 104;
pub const L1_VALIDATOR_SIZE: usize = 112;

/// A view of a ZAP-encoded L1Validator. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct L1Validator<'a> {
    o: zap::Object<'a>,
}

impl<'a> L1Validator<'a> {
    /// Take `data` as a L1Validator message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(L1Validator {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        L1Validator { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    /// The 32 inline bytes at `L1_VALIDATOR_CHAIN`; zeros if the span runs off the buffer.
    pub fn chain(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(L1_VALIDATOR_CHAIN, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    /// The 20 inline bytes at `L1_VALIDATOR_NODE_ID`; zeros if the span runs off the buffer.
    pub fn node_id(&self) -> &'a [u8; 20] {
        self.o
            .bytes_fixed(L1_VALIDATOR_NODE_ID, 20)
            .try_into()
            .unwrap_or(&[0u8; 20])
    }

    pub fn start(&self) -> u64 {
        self.o.u64(L1_VALIDATOR_START)
    }

    pub fn weight(&self) -> u64 {
        self.o.u64(L1_VALIDATOR_WEIGHT)
    }

    pub fn min_nonce(&self) -> u64 {
        self.o.u64(L1_VALIDATOR_MIN_NONCE)
    }

    pub fn end_fee(&self) -> u64 {
        self.o.u64(L1_VALIDATOR_END_FEE)
    }

    pub fn public_key(&self) -> &'a [u8] {
        self.o.bytes(L1_VALIDATOR_PUBLIC_KEY)
    }

    pub fn remaining_owner(&self) -> &'a [u8] {
        self.o.bytes(L1_VALIDATOR_REMAINING_OWNER)
    }

    pub fn disable_owner(&self) -> &'a [u8] {
        self.o.bytes(L1_VALIDATOR_DISABLE_OWNER)
    }
}

/// The field values [`new_l1_validator`] writes.
#[derive(Clone, Copy, Debug)]
pub struct L1ValidatorInput<'a> {
    pub chain: &'a [u8; 32],
    pub node_id: &'a [u8; 20],
    pub start: u64,
    pub weight: u64,
    pub min_nonce: u64,
    pub end_fee: u64,
    pub public_key: &'a [u8],
    pub remaining_owner: &'a [u8],
    pub disable_owner: &'a [u8],
}

impl<'a> Default for L1ValidatorInput<'a> {
    fn default() -> Self {
        L1ValidatorInput {
            chain: &[0u8; 32],
            node_id: &[0u8; 20],
            start: 0,
            weight: 0,
            min_nonce: 0,
            end_fee: 0,
            public_key: &[],
            remaining_owner: &[],
            disable_owner: &[],
        }
    }
}

/// Write a L1Validator into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_l1_validator(b: &mut zap::Builder, input: &L1ValidatorInput<'_>) -> usize {
    let mut ob = b.start_object(L1_VALIDATOR_SIZE);
    ob.set_bytes_fixed(b, L1_VALIDATOR_CHAIN, input.chain);
    ob.set_bytes_fixed(b, L1_VALIDATOR_NODE_ID, input.node_id);
    ob.set_u64(b, L1_VALIDATOR_START, input.start);
    ob.set_u64(b, L1_VALIDATOR_WEIGHT, input.weight);
    ob.set_u64(b, L1_VALIDATOR_MIN_NONCE, input.min_nonce);
    ob.set_u64(b, L1_VALIDATOR_END_FEE, input.end_fee);
    ob.set_bytes(b, L1_VALIDATOR_PUBLIC_KEY, input.public_key);
    ob.set_bytes(b, L1_VALIDATOR_REMAINING_OWNER, input.remaining_owner);
    ob.set_bytes(b, L1_VALIDATOR_DISABLE_OWNER, input.disable_owner);
    ob.finish(b)
}

/// Write a L1Validator message and answer its bytes.
pub fn new_l1_validator(input: &L1ValidatorInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_l1_validator(&mut b, input);
    b.set_root(at);
    b.finish()
}

// Conversion — field offsets, in bytes, and the fixed section's size.
pub const CONVERSION_CONVERSION_ID: usize = 0;
pub const CONVERSION_CHAIN: usize = 32;
pub const CONVERSION_ADDRESS: usize = 64;
pub const CONVERSION_SIZE: usize = 72;

/// A view of a ZAP-encoded Conversion. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Conversion<'a> {
    o: zap::Object<'a>,
}

impl<'a> Conversion<'a> {
    /// Take `data` as a Conversion message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Conversion {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Conversion { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    /// The 32 inline bytes at `CONVERSION_CONVERSION_ID`; zeros if the span runs off the buffer.
    pub fn conversion_id(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(CONVERSION_CONVERSION_ID, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    /// The 32 inline bytes at `CONVERSION_CHAIN`; zeros if the span runs off the buffer.
    pub fn chain(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(CONVERSION_CHAIN, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    pub fn address(&self) -> &'a [u8] {
        self.o.bytes(CONVERSION_ADDRESS)
    }
}

/// The field values [`new_conversion`] writes.
#[derive(Clone, Copy, Debug)]
pub struct ConversionInput<'a> {
    pub conversion_id: &'a [u8; 32],
    pub chain: &'a [u8; 32],
    pub address: &'a [u8],
}

impl<'a> Default for ConversionInput<'a> {
    fn default() -> Self {
        ConversionInput {
            conversion_id: &[0u8; 32],
            chain: &[0u8; 32],
            address: &[],
        }
    }
}

/// Write a Conversion into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_conversion(b: &mut zap::Builder, input: &ConversionInput<'_>) -> usize {
    let mut ob = b.start_object(CONVERSION_SIZE);
    ob.set_bytes_fixed(b, CONVERSION_CONVERSION_ID, input.conversion_id);
    ob.set_bytes_fixed(b, CONVERSION_CHAIN, input.chain);
    ob.set_bytes(b, CONVERSION_ADDRESS, input.address);
    ob.finish(b)
}

/// Write a Conversion message and answer its bytes.
pub fn new_conversion(input: &ConversionInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_conversion(&mut b, input);
    b.set_root(at);
    b.finish()
}

// Change — field offsets, in bytes, and the fixed section's size.
pub const CHANGE_AMOUNT: usize = 0;
pub const CHANGE_DECREASE: usize = 8;
pub const CHANGE_RENAMED: usize = 9;
pub const CHANGE_VALIDATION: usize = 16;
pub const CHANGE_KEY_BEFORE: usize = 48;
pub const CHANGE_KEY_AFTER: usize = 56;
pub const CHANGE_SIZE: usize = 64;

/// A view of a ZAP-encoded Change. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Change<'a> {
    o: zap::Object<'a>,
}

impl<'a> Change<'a> {
    /// Take `data` as a Change message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Change {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Change { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn amount(&self) -> u64 {
        self.o.u64(CHANGE_AMOUNT)
    }

    pub fn decrease(&self) -> u8 {
        self.o.u8(CHANGE_DECREASE)
    }

    pub fn renamed(&self) -> u8 {
        self.o.u8(CHANGE_RENAMED)
    }

    /// The 32 inline bytes at `CHANGE_VALIDATION`; zeros if the span runs off the buffer.
    pub fn validation(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(CHANGE_VALIDATION, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    pub fn key_before(&self) -> &'a [u8] {
        self.o.bytes(CHANGE_KEY_BEFORE)
    }

    pub fn key_after(&self) -> &'a [u8] {
        self.o.bytes(CHANGE_KEY_AFTER)
    }
}

/// The field values [`new_change`] writes.
#[derive(Clone, Copy, Debug)]
pub struct ChangeInput<'a> {
    pub amount: u64,
    pub decrease: u8,
    pub renamed: u8,
    pub validation: &'a [u8; 32],
    pub key_before: &'a [u8],
    pub key_after: &'a [u8],
}

impl<'a> Default for ChangeInput<'a> {
    fn default() -> Self {
        ChangeInput {
            amount: 0,
            decrease: 0,
            renamed: 0,
            validation: &[0u8; 32],
            key_before: &[],
            key_after: &[],
        }
    }
}

/// Write a Change into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_change(b: &mut zap::Builder, input: &ChangeInput<'_>) -> usize {
    let mut ob = b.start_object(CHANGE_SIZE);
    ob.set_u64(b, CHANGE_AMOUNT, input.amount);
    ob.set_u8(b, CHANGE_DECREASE, input.decrease);
    ob.set_u8(b, CHANGE_RENAMED, input.renamed);
    ob.set_bytes_fixed(b, CHANGE_VALIDATION, input.validation);
    ob.set_bytes(b, CHANGE_KEY_BEFORE, input.key_before);
    ob.set_bytes(b, CHANGE_KEY_AFTER, input.key_after);
    ob.finish(b)
}

/// Write a Change message and answer its bytes.
pub fn new_change(input: &ChangeInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_change(&mut b, input);
    b.set_root(at);
    b.finish()
}

