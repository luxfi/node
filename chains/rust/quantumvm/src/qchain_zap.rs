// Code generated by zapgen; DO NOT EDIT.
// source: qchain.zap

//! ZAP schema `qchain.zap`, package `qchain`.
//!
//! Every view here borrows the buffer it reads: a field is a bounds-checked
//! look at bytes already in hand, never a decode into an owned struct. The
//! builders write into one buffer and hand it over at the end.

#![allow(dead_code)]

use lux_zap::zap;

// Block — field offsets, in bytes, and the fixed section's size.
pub const BLOCK_TIME: usize = 0;
pub const BLOCK_HEIGHT: usize = 8;
pub const BLOCK_PARENT: usize = 16;
pub const BLOCK_CHAIN: usize = 48;
pub const BLOCK_NETWORK: usize = 80;
pub const BLOCK_TX_LENGTHS: usize = 88;
pub const BLOCK_TX_BLOB: usize = 96;
pub const BLOCK_SIZE: usize = 104;

/// A view of a ZAP-encoded Block. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Block<'a> {
    o: zap::Object<'a>,
}

impl<'a> Block<'a> {
    /// Take `data` as a Block message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Block {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Block { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn time(&self) -> i64 {
        self.o.i64(BLOCK_TIME)
    }

    pub fn height(&self) -> u64 {
        self.o.u64(BLOCK_HEIGHT)
    }

    /// The 32 inline bytes at `BLOCK_PARENT`; zeros if the span runs off the buffer.
    pub fn parent(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(BLOCK_PARENT, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    /// The 32 inline bytes at `BLOCK_CHAIN`; zeros if the span runs off the buffer.
    pub fn chain(&self) -> &'a [u8; 32] {
        self.o
            .bytes_fixed(BLOCK_CHAIN, 32)
            .try_into()
            .unwrap_or(&[0u8; 32])
    }

    pub fn network(&self) -> u32 {
        self.o.u32(BLOCK_NETWORK)
    }

    pub fn tx_lengths(&self) -> zap::List<'a> {
        self.o.list_stride(BLOCK_TX_LENGTHS, 4)
    }

    pub fn tx_blob(&self) -> &'a [u8] {
        self.o.bytes(BLOCK_TX_BLOB)
    }
}

/// The field values [`new_block`] writes.
#[derive(Clone, Copy, Debug)]
pub struct BlockInput<'a> {
    pub time: i64,
    pub height: u64,
    pub parent: &'a [u8; 32],
    pub chain: &'a [u8; 32],
    pub network: u32,
    pub tx_lengths: &'a [u32],
    pub tx_blob: &'a [u8],
}

impl<'a> Default for BlockInput<'a> {
    fn default() -> Self {
        BlockInput {
            time: 0,
            height: 0,
            parent: &[0u8; 32],
            chain: &[0u8; 32],
            network: 0,
            tx_lengths: &[],
            tx_blob: &[],
        }
    }
}

/// Write a Block into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_block(b: &mut zap::Builder, input: &BlockInput<'_>) -> usize {
    let mut list_tx_lengths = b.start_list();
    for elem in input.tx_lengths {
        list_tx_lengths.add_u32(b, *elem);
    }
    let at_tx_lengths = list_tx_lengths.finish_offset();
    let mut ob = b.start_object(BLOCK_SIZE);
    ob.set_i64(b, BLOCK_TIME, input.time);
    ob.set_u64(b, BLOCK_HEIGHT, input.height);
    ob.set_bytes_fixed(b, BLOCK_PARENT, input.parent);
    ob.set_bytes_fixed(b, BLOCK_CHAIN, input.chain);
    ob.set_u32(b, BLOCK_NETWORK, input.network);
    ob.set_list(b, BLOCK_TX_LENGTHS, at_tx_lengths, input.tx_lengths.len());
    ob.set_bytes(b, BLOCK_TX_BLOB, input.tx_blob);
    ob.finish(b)
}

/// Write a Block message and answer its bytes.
pub fn new_block(input: &BlockInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_block(&mut b, input);
    b.set_root(at);
    b.finish()
}

// Body — field offsets, in bytes, and the fixed section's size.
pub const BODY_TIME: usize = 0;
pub const BODY_NONCE: usize = 8;
pub const BODY_DATA: usize = 16;
pub const BODY_SIZE: usize = 24;

/// A view of a ZAP-encoded Body. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Body<'a> {
    o: zap::Object<'a>,
}

impl<'a> Body<'a> {
    /// Take `data` as a Body message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Body {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Body { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn time(&self) -> i64 {
        self.o.i64(BODY_TIME)
    }

    pub fn nonce(&self) -> u64 {
        self.o.u64(BODY_NONCE)
    }

    pub fn data(&self) -> &'a [u8] {
        self.o.bytes(BODY_DATA)
    }
}

/// The field values [`new_body`] writes.
#[derive(Clone, Copy, Debug)]
pub struct BodyInput<'a> {
    pub time: i64,
    pub nonce: u64,
    pub data: &'a [u8],
}

impl<'a> Default for BodyInput<'a> {
    fn default() -> Self {
        BodyInput {
            time: 0,
            nonce: 0,
            data: &[],
        }
    }
}

/// Write a Body into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_body(b: &mut zap::Builder, input: &BodyInput<'_>) -> usize {
    let mut ob = b.start_object(BODY_SIZE);
    ob.set_i64(b, BODY_TIME, input.time);
    ob.set_u64(b, BODY_NONCE, input.nonce);
    ob.set_bytes(b, BODY_DATA, input.data);
    ob.finish(b)
}

/// Write a Body message and answer its bytes.
pub fn new_body(input: &BodyInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_body(&mut b, input);
    b.set_root(at);
    b.finish()
}

// Tx — field offsets, in bytes, and the fixed section's size.
pub const TX_BODY: usize = 0;
pub const TX_ALGORITHM: usize = 8;
pub const TX_STAMPED: usize = 16;
pub const TX_PUBLIC_KEY: usize = 24;
pub const TX_SIGNATURE: usize = 32;
pub const TX_STAMP: usize = 40;
pub const TX_SIZE: usize = 48;

/// A view of a ZAP-encoded Tx. Reading a field costs a bounds check.
#[derive(Clone, Copy, Debug)]
pub struct Tx<'a> {
    o: zap::Object<'a>,
}

impl<'a> Tx<'a> {
    /// Take `data` as a Tx message. Fails only on the wire-level
    /// checks — magic, version, declared size.
    pub fn wrap(data: &'a [u8]) -> Result<Self, zap::Error> {
        Ok(Tx {
            o: zap::Message::parse(data)?.root(),
        })
    }

    /// A view of an object already located in a message — a nested
    /// field, or one element of a list.
    pub fn new(o: zap::Object<'a>) -> Self {
        Tx { o }
    }

    /// The object this view reads.
    pub fn object(&self) -> zap::Object<'a> {
        self.o
    }

    pub fn body(&self) -> &'a [u8] {
        self.o.bytes(TX_BODY)
    }

    pub fn algorithm(&self) -> u32 {
        self.o.u32(TX_ALGORITHM)
    }

    pub fn stamped(&self) -> i64 {
        self.o.i64(TX_STAMPED)
    }

    pub fn public_key(&self) -> &'a [u8] {
        self.o.bytes(TX_PUBLIC_KEY)
    }

    pub fn signature(&self) -> &'a [u8] {
        self.o.bytes(TX_SIGNATURE)
    }

    pub fn stamp(&self) -> &'a [u8] {
        self.o.bytes(TX_STAMP)
    }
}

/// The field values [`new_tx`] writes.
#[derive(Clone, Copy, Debug)]
pub struct TxInput<'a> {
    pub body: &'a [u8],
    pub algorithm: u32,
    pub stamped: i64,
    pub public_key: &'a [u8],
    pub signature: &'a [u8],
    pub stamp: &'a [u8],
}

impl<'a> Default for TxInput<'a> {
    fn default() -> Self {
        TxInput {
            body: &[],
            algorithm: 0,
            stamped: 0,
            public_key: &[],
            signature: &[],
            stamp: &[],
        }
    }
}

/// Write a Tx into `b` and answer where its object landed.
///
/// What a field points AT is written first, in field order, and the
/// fixed section last: a pointer always leads backward, to bytes
/// already placed.
pub fn put_tx(b: &mut zap::Builder, input: &TxInput<'_>) -> usize {
    let mut ob = b.start_object(TX_SIZE);
    ob.set_bytes(b, TX_BODY, input.body);
    ob.set_u32(b, TX_ALGORITHM, input.algorithm);
    ob.set_i64(b, TX_STAMPED, input.stamped);
    ob.set_bytes(b, TX_PUBLIC_KEY, input.public_key);
    ob.set_bytes(b, TX_SIGNATURE, input.signature);
    ob.set_bytes(b, TX_STAMP, input.stamp);
    ob.finish(b)
}

/// Write a Tx message and answer its bytes.
pub fn new_tx(input: &TxInput<'_>) -> Vec<u8> {
    let mut b = zap::Builder::new_v2(256);
    let at = put_tx(&mut b, input);
    b.set_root(at);
    b.finish()
}

