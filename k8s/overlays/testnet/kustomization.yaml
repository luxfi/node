apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: lux-testnet

labels:
- pairs:
    app.kubernetes.io/instance: testnet

patches:
- path: patches.yaml
