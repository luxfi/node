apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: lux-devnet

labels:
- pairs:
    app.kubernetes.io/instance: devnet

patches:
- path: patches.yaml
